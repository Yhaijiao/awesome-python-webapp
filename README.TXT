Only if there's 1 file in the directory, it is able to push it on to the git hub.
1. git add . //add all changed file to git
2. git commit -m "what's the updates"
3. git push origin_awesome-python-webapp master 